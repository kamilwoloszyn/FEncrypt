import React from "react";
import ReactDOM from "react-dom";

interface Props {
  text:string
}

export const Uploader: React.FC<Props> = ({}) => {
  return (
    <h1> Hello from Uploader module</h1>
  )
} 

